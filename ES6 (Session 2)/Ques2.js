// Find the possible combinations of a string and store them in a MAP

let map = new Map();

map.set('1', 'str');   // with a string key
map.set(1, 'num');     // with a numeric key
map.set(true, 'bool'); // with a boolean key
